<UserControl x:Class="Navigation_WPF.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Navigation_WPF.Views"
             xmlns:vm="clr-namespace:Navigation_WPF.ViewModels"
             mc:Ignorable="d" Background="Transparent"
             d:DesignHeight="600" d:DesignWidth="622">
    <UserControl.DataContext>
        <vm:HomeViewModel/>
    </UserControl.DataContext>
    <Grid>
        <Border CornerRadius="0 20 20 0">
            <Border.Background>
                <ImageBrush ImageSource="../Themes/Images/home_page.png" Stretch="None"/>
            </Border.Background>
        </Border>
    </Grid>
</UserControl>
